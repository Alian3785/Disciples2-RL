# Исправленные баги

Формат: дата — баг — суть исправления — файлы — тесты.

- 2026-07-04 — **№1: Призыв юнитов типа Summoner не срабатывал ни у одной из команд** — сравнение `unit_type == "summoner"` не совпадало с типом `"Summoner"` из данных (маска синих, исполнение действия, ИИ красных); добавлен регистронезависимый хелпер `_is_summoner_type`, плюс ранний `return mask` в блоке маски призывателя (её затирала общая ветка «любые живые цели»). Заодно закреплены решения: призванные дают 0 опыта (`exp_kill: 0` в шаблонах — анти-фарм, задокументированное отклонение от оригинала), а опыт трупов, затёртых призывом в их клетку, копится в `overwritten_exp_kill` и учитывается в `_apply_battle_exp` — `battle_env.py` (`_is_summoner_type`, `_accumulate_overwritten_exp`, маска ~1860, `_attack` ~4880, ИИ красных ~5395), `summon_actions.py` (`_spawn_occultmaster_unit`) — `tests/test_summoner_summons.py` (5 тестов). Верификация: битва «Мудрец+Грифон vs Дракон рока+Патриарх», 200 боёв, сид 5000: до фикса 296/300 побед случайной политики, после — 180/200 (бит-в-бит с эталонной эмуляцией фикса).
- 2026-07-04 — **Данные: нулевой опыт за Дриаду и Мудреца** — при переносе из Gunits.dbf колонки уехали на одну: XP_KILLED попал в «нужный опыт», XP_NEXT — в «текущий опыт», а «опыт убийства» остался 0; выправлено по оригиналу (Дриада: 66/165/0, Мудрец: 36/100/0; HP 55/80 в DBF совпали — юниты те же) — `data_dicts_compact_lines.py` (строки Дриады и Мудреца) — базовые тесты: test_data_dicts_compact_lines, test_summoner_summons, test_enemy_exp_mapping (10 passed).
- 2026-07-04 — **Затирание трупов призывом теряло опыт победителя** — закрыт полностью: накопитель `overwritten_exp_kill` (учитывается в `_apply_battle_exp`) покрывал пути призывателя и оккультиста/Лифа с фикса №1, добавлен третий путь — призыв предметом героя (`_apply_hero_item_summon`, battle_env.py:1481); призванные при этом дают 0 опыта, фарм невозможен — `battle_env.py` — тест `test_hero_item_summon_overwrite_preserves_corpse_exp` (+35 базовых passed). Сквозная верификация: Лиф+Ониксовая гаргулья vs Утер демон, 100 боёв: 28 затираний трупов, расхождений опыта 0 (гаргулья 810 доходит до победителя через банк).
- 2026-07-04 — **№2: DOT-эффекты молча пропускали тик при инициативе юнита <= 10** — проверка `initiative > 10` (защита от двойного тика после «ждать») отключала яд/поджиг/воду, снятие замедления Отшельника и откаты превращений юнитам с низкой инициативой (Патриарх/жрецы/Дева рощи/Алхимик при броске +0 — каждый ~10-й раунд); заменена явным флагом `round_effects_done` на юните: ставится при первой активации в раунде, сбрасывается в `_end_round_restore` — `battle_env.py` (`_apply_start_of_turn_effects`: 5 условий, `_end_round_restore`, инициализация в `_reset_state`/`_init_with_custom_teams`) — `tests/test_dot_round_flag.py` (3 теста: тик при иниц. 10, отсутствие двойного тика после «ждать» + сброс новым раундом, откат превращения при низкой инициативе); обновлены 2 устаревших теста и проба, полагавшиеся на старый костыль. Базовые тесты: 25 passed.
- 2026-07-04 — **№3: красный Двойник атаковал вместо копирования (в т.ч. своих)** — ИИ выбирал цель для копирования (макс. HP non-big по обеим командам), но исполнял выбор общей веткой `_attack` — Двойник бил выбранного юнита, включая собственных союзников; копирование существовало только в пути агента. Вынесено в общий метод `_apply_doppelganger_copy`, красная ветка копирует; запретный список стражей вынесен в `DOPPELGANGER_FORBIDDEN_COPY_NAMES` и применён и к выбору цели ИИ. Попутно: переключатель «нет целей - тип Warrior» откатывал тип у копий воинов (у обеих сторон) — копия помечается флагом `doppel_copied`, переключатель её не трогает; флаг сбрасывается в `_restore_default_doppelgangers` и на старте боя — `battle_env.py` — `tests/test_doppelganger_red_copy.py` (3 теста; 19 базовых passed). Верификация: битва «гаргулья+Двойник+Инкуб+Суккуб vs эльфы/гномы», 200 боёв, те же сиды: победы красных 12% до фикса > 57% после.
- 2026-07-04 — **№4: после отката превращения юнит навсегда оставался медленным (initiative_base 30/50)** — рукописный снапшот в `_apply_witch_effect` не содержал `initiative_base`, который эффект перезаписывает; откат восстанавливал только поля из снапшота, и порча утекала в persistent-состав отряда кампании через `_save_blue_state` (юнит агента становился вдвое медленнее навсегда). Затронуты были все 4 пути превращения: Ведьма/Колдунья, Суккуб, предмет героя, боевой артефакт. Фикс — поле добавлено в снапшот (одна строка) — `battle_env.py` — `tests/test_witch_transform_restore.py` (откат в бою + откат в конце боя; 19 базовых passed). Странность «полная инициатива после отката = возможный лишний ход» вынесена в TODO для сверки с оригиналом.
