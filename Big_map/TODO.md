# TODO

## Отличия от оригинальной Disciples II

- [ ] **Добавить настоящий туман войны.**
  Сейчас агент с начала эпизода получает через observation координаты врагов, сундуков,
  руин и других объектов. В оригинале обзор ограничен туманом войны, а способность
  `Keen-Sight` увеличивает дальность видимости. См. [`campaign_env_observation.py`](campaign_env_observation.py#L74)
  и руководство Disciples II.

- [ ] **Добавить выбор способности героя при повышении уровня.**
  Сейчас способности открываются автоматически на фиксированных уровнях: поиск пути на
  2-м, лидерство на 3-м, выносливость на 4-м, сила на 5-м и далее. В оригинале после
  повышения уровня игрок выбирает способность. См. [`campaign_env_data.py`](campaign_env_data.py#L177).

- [ ] **Разделить рост характеристик по типам лидеров и выбранным способностям.**
  Сейчас рост героя унифицирован: примерно `+10%` к HP и урону, `+1` к точности и
  фиксированные milestone-бонусы. В оригинале развитие зависит от типа лидера и выбранных
  способностей. См. [`battle_env.py`](battle_env.py#L572).

- [ ] **Добавить выбор цели атакующего заклинания на глобальной карте.**
  Сейчас автоматически выбирается ближайший допустимый вражеский отряд. В оригинале игрок
  сам указывает допустимую цель. См. [`campaign_env_magic.py`](campaign_env_magic.py#L237).

- [ ] **Реализовать оставшиеся неподдерживаемые эффекты свитков.**
  Из 120 описаний сейчас поддерживаются 99, а 21 помечено `unsupported`. В основном это
  нестандартные модификаторы и иллюзорные призывы. См. [`scroll_spell_data.py`](scroll_spell_data.py#L5).

- [ ] **Добавить ограничение вместимости походного инвентаря и выбрасывание предметов.**
  Сейчас инвентарь неограничен, а предмет нельзя оставить на карте в виде мешка. См.
  [`inventorybugs/README.md`](inventorybugs/README.md#L46).

- [ ] **Убрать автоматическую экипировку полученных артефактов, книг и ботинок.**
  В оригинале игрок вручную перекладывает предмет в соответствующий слот. См.
  [`campaign_env_inventory.py`](campaign_env_inventory.py#L1668).

- [ ] **Убрать искусственный лимит экипировки за игровой ход.**
  Сейчас за ход разрешено только одно действие экипировки боевого предмета и одно действие
  экипировки книги. Это ограничение action space для обучения, а не правило оригинала. См.
  [`campaign_env_inventory.py`](campaign_env_inventory.py#L2327).

- [ ] **Добавить ручное подтверждение продажи драгоценностей торговцу.**
  Сейчас драгоценности автоматически продаются при входе к торговцу. См.
  [`campaign_env_inventory.py`](campaign_env_inventory.py#L2111).

- [ ] **Добавить изменяемый городской гарнизон.**
  Сейчас нельзя переносить юнитов между походным отрядом и защитниками города, а защитники
  остаются статической частью города. См. [`economybugs/README.md`](economybugs/README.md#L51).

- [ ] **Пересчитывать скорость расширения территории после повышения уровня города.**
  Само повышение города с 1-го до 5-го уровня реализовано, броня и лечение обновляются,
  но `expansion_per_turn` сохраняет значение, рассчитанное для исходного уровня карты. См.
  [`campaign_env_economy.py`](campaign_env_economy.py#L3197) и [`grid.py`](grid.py#L656).

- [ ] **Добавить действие увольнения обычного юнита.**
  Сейчас состав меняется наймом, гибелью и заменой, но обычного юнита нельзя вручную
  распустить.

- [ ] **Сверить опыт за призванных в бою существ с оригиналом.**
  Сейчас они дают `0` опыта как специальная защита от фарма; это сознательное отклонение
  от оригинала. См. [`FIXED_BUGS.md`](FIXED_BUGS.md#L5).

- [ ] **Отделить специальную полную регенерацию целевого дракона от общих правил.**
  Сейчас стек `id=31` полностью восстанавливает здоровье каждый новый игровой ход в режимах
  Зелёного и Синего дракона. Это специальное правило среды, а не стандартное поведение
  оригинала. См. [`campaign_env_magic.py`](campaign_env_magic.py#L1916).

- [ ] **Сделать технические пределы длины эпизода настраиваемыми отдельно от правил карты.**
  Сейчас обучение ограничено 5000 общими шагами и 3000 grid-шагами, тогда как в оригинальной
  партии такого предела нет. См. [`train_campaign.py`](train_campaign.py#L148).

## Среда / бои

- [ ] **Добавить изменение опыта за убийство и брони при динамическом повышении уровня.**
  При level-up нейтралов, боссов и конечных форм увеличивать `exp_kill`
  вместе с боевыми характеристиками. Рост брони применять не ко всем бронированным
  юнитам, а только к существам с соответствующим профилем `ARMOR` в оригинальной
  `GDynUpgr.DBF` (обычно `+1` за уровень до 10-го). Добавить данные профилей,
  сохранение изменений в кампании и отдельные тесты для обоих параметров.

- [ ] **Сверить с оригиналом: лишний ход после отката превращения.**
  В снапшоте витч-эффекта (`_apply_witch_effect`, battle_env.py) ключ
  `"initiative"` хранит старую БАЗУ инициативы, а не текущую инициативу жертвы.
  После отката превращения юнит получает полную инициативу раунда — даже если
  в этом раунде уже ходил, т.е. может получить лишний ход в раунд излечения.
  Проверить в оригинальной D2 и решить: хранить текущую инициативу или оставить.

- [ ] **Проверить стык Суккуб × Двойник (конфигурация №18 из плана битв).**
  Скопированного Двойника превращает Суккуб: поверх копии ложится снапшот
  витч-эффекта (`basestats`), а в конце боя откаты идут в порядке
  `_restore_default_doppelgangers` (по имени, канонические статы) →
  `_restore_all_transformed_units` (по снапшоту) — могут перезаписать друг друга.
  Прогнать `tools/sim_bughunt_battle.py --red "2:Суккуб,5:Колдунья" --blue
  "8:Двойник,7:Мастер клинка"` и проверить статы Двойника после боя.

## Обучение

- [x] **Добавить лимит полной длины эпизода в обучение.** (сделано 2026-06-22)
  Тренировочный env теперь оборачивается в `EvalStepCapWrapper(max_steps=TRAIN_MAX_EPISODE_STEPS=5000)`
  в `make_env` (train_campaign.py) — жёсткий потолок всех env-шагов (карта + бои).
  Также: `MAX_EVAL_EPISODE_STEPS = 5000` (eval), `max_grid_steps = 3000` (потолок шагов движения).
  Контекст: без этого эпизод мог не завершаться, т.к. `max_grid_steps`/`step_count` растёт только
  на движении/отдыхе (0–8), а касты/стройка/найм его не двигают.
